apiVersion: v1
kind: Pod
metadata:
 name: readiness-pod
 labels:
   app: app
spec:
 containers:
 - name: readiness
   image: nginx:latest
   ports:
   - containerPort: 8080
   readinessProbe:
     httpGet:
       path: /
       port: 80
     periodSeconds: 10
