apiVersion: v1
kind: Pod
metadata:
  name: multi-labeled-pod
  labels:
    app: myapp
    environment: prod
    team: testers
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
      - containerPort: 80
